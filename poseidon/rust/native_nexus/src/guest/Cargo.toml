[package]
name = "guest"
version = "0.1.0"
edition = "2021"

[dependencies]
nexus-rt = { git = "https://github.com/nexus-xyz/nexus-zkvm.git", tag = "0.2.3", version = "0.2.3" }
postcard = { version = "=1.0.8", default-features = false, features = ["alloc"] }
poseidon-ark = { git = "https://github.com/arnaucube/poseidon-ark" }
ark-ff = { version = "0.4", default-features = false }
ark-bn254 = { version = "0.4", default-features = false }
ark-std = { version = "0.4", default-features = false }
# once_cell = { version = "*", default-features = false, features = [] }

# Generated by cargo-nexus, do not remove!
#
[features]
cycles = [] # Enable cycle counting for run command

